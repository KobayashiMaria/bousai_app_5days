# 避難所検索機能を移植するための実装プロンプト

あなたは既存の防災Webアプリを読み取り、避難所検索機能一式を実装するシニアWebエンジニアです。
以下の要件をすべて満たすように、対象プロジェクトの既存構成・既存デザイン・既存ルーティングを尊重して実装してください。
この指示への確認質問はせず、まずリポジトリを調査し、必要なファイルを作成・編集して、最後に動作確認まで行ってください。

## 目的

住民が避難所を探せる、次の3つの機能を追加・完成させてください。

1. 避難所検索画面
2. 現在地を利用した簡易検索
3. 条件を指定できる詳細検索

検索結果は専用の結果画面に表示し、検索条件変更・検索画面への戻り・トップページへの戻りもできるようにしてください。

## 前提技術

- バックエンドは既存プロジェクトのフレームワークを使用すること。
- この構成がFlaskの場合は、既存の `app.py`、`templates`、`static`、`data` の構成に合わせること。
- 追加ライブラリは必要最小限にし、現在地取得はブラウザ標準のGeolocation APIを使用すること。
- 既存のトップページ、ログイン、避難所登録、指示ボード、気象情報などの無関係な機能を壊さないこと。
- 既存のヘッダー、ナビゲーション、色、余白、レスポンシブ方針に合わせること。
- 日本語UI、`lang="ja"`、スマートフォン表示に対応すること。

## 必要なURL・画面

### 1. 避難所検索画面

`/shelter_search` など、既存プロジェクトの規約に合うURLを用意してください。

画面には次を、この順序で表示してください。

- ページ見出し「避難所検索」
- 「簡易検索」セクション
- 「詳細検索」セクション
- 「登録されている避難所をすべて見る」リンク

既存の共通レイアウトを継承し、フォームのラベルと入力欄が明確に対応するようにしてください。

### 2. 簡易検索

「最寄りの避難所を検索する」ボタンを設置してください。

ボタン押下時の処理は次のとおりです。

- `navigator.geolocation` が利用できない場合は送信を中止し、「このブラウザでは現在地を取得できません。」と表示する。
- 位置情報取得中は「現在地を確認しています...」と表示する。
- 位置情報の取得に成功したら、緯度を `latitude`、経度を `longitude` としてGETパラメータに設定し、検索結果URLへ送信する。
- 位置情報の取得に失敗した場合は送信を中止し、「現在地を取得できませんでした。位置情報を許可してください。」と表示する。
- 二重送信を避けるため、座標が未取得の状態では一度だけ位置情報を取得してからフォームを送信する。
- ブラウザの権限拒否や未対応を、画面内のステータスメッセージで利用者に伝える。

### 3. 詳細検索

同じ検索結果URLへGETで送信するフォームを作成してください。

#### 避難所名

- ラベル「避難所名：」
- `name` パラメータ
- 自由入力の検索欄
- 避難所名の部分一致で検索する
- 大文字・小文字の違いに左右されないようにする
- 空欄の場合は避難所名で絞り込まない

#### 出発地点

ラジオボタンとして次を用意してください。

- 「指定しない」: `none`、初期選択
- 「現在地」: `current`
- 「自宅」: `home`

現時点で自宅情報を保存する既存機能がない場合でも、UIは用意してください。未実装の選択肢を silently ignore せず、既存データ構造で実現できる範囲を説明し、将来拡張可能な形で受け取ってください。現在地を選択した場合に実際の座標検索まで実装するなら、Geolocation APIと同じエラーメッセージ・送信フローを使ってください。

#### 配慮事項

複数選択できるチェックボックスとして、パラメータ名 `concern` で次を用意してください。

- `高齢者（65歳以上）`
- `子供連れ`
- `障害者・要介護者`
- `外国人`
- `ペット`

複数選択時は、選択されたすべての配慮事項に対応している避難所だけを返してください（AND条件）。

### 4. 検索結果画面

検索結果URLは、既存プロジェクトの規約に合わせて `/search_results` などにしてください。

次の表示を実装してください。

- 現在地検索の場合の見出し「最寄りの避難所」
- それ以外の場合の見出し「検索結果」
- 現在地検索であることが分かる場合は、見出しを適切に切り替える
- 検索条件変更リンク
- 結果件数「N件の避難所が見つかりました。」
- 各結果の避難所名
- 住所が存在する場合の住所
- 結果が0件の場合の「条件に一致する避難所が見つかりませんでした。」
- 「検索画面に戻る」リンク
- 「トップページへ戻る」リンク

テンプレートへ渡す値は必ずエスケープし、避難所データに欠損項目があっても画面が壊れないようにしてください。

## バックエンドの検索仕様

検索処理はテンプレート内に埋め込まず、既存バックエンドに再利用可能な検索関数として実装してください。

想定する関数の責務は次のとおりです。

```text
search_shelters(name='', concerns=None, latitude=None, longitude=None)
```

処理順序は次のとおりです。

1. 全避難所を読み込む。
2. `name` が指定されていれば、避難所名の部分一致で絞り込む。
3. `concerns` が1件以上あれば、すべての項目を満たす避難所だけに絞り込む。
4. `latitude` と `longitude` が有効なら、避難所の座標から距離を計算する。
5. 現在地の座標がある場合は、距離の近い順に並べる。
6. 座標がない避難所は距離順の最後に置き、検索処理自体は失敗させない。
7. 座標が不正・欠損の場合は、距離順を適用せず、検索結果を返す。

距離は、少なくとも検索結果の近い順を安定して決められる方法で計算してください。外部地図APIやAPIキーは必須にしないでください。地理的な正確な距離が必要な場合は、既存プロジェクトの依存関係に合わせてHaversine距離などを使用してください。

HTTP GETパラメータは次の契約にしてください。

```text
/search_results?name=...&concern=...&concern=...&latitude=...&longitude=...
```

- `concern` は複数値を受け取れること。
- 欠損・空文字・不正な数値に対して500エラーを返さないこと。
- 検索結果がない場合も正常な結果画面を返すこと。
- 全避難所一覧リンクでは検索条件を使わず、全件を表示すること。

## 避難所データ契約

既存のJSON、DB、APIなどのデータソースを優先して利用してください。
JSONを使う場合、既存の項目を壊さず、以下のような項目を扱えるようにしてください。

```json
{
  "id": 1,
  "name": "避難所名",
  "address": "住所",
  "latitude": 40.000000,
  "longitude": 140.000000,
  "features": ["高齢者（65歳以上）", "ペット"]
}
```

配慮事項のフィールド名がプロジェクト内で `features`、`considerations` など異なる場合は、既存形式を優先し、互換性を保ってください。文字列1件が入っている場合も配列として扱えるようにしてください。

テスト・動作確認用に、検索条件が実際に意味を持つサンプルデータを用意してください。少なくとも、次のケースを確認できるデータにしてください。

- 複数の避難所名
- 住所あり・住所なし
- 緯度経度あり・なし
- 配慮事項が異なる避難所
- 配慮事項を複数持つ避難所

既存データを利用する場合、既存ユーザーが登録した内容を勝手に削除しないでください。

## ナビゲーション

共通ヘッダーまたは既存メニューから「避難所検索」へ移動できるようにしてください。
検索結果画面から検索画面、トップページへ戻れるようにしてください。
URLをハードコードせず、Flaskなら `url_for` など既存フレームワークのURL生成機能を使ってください。

## レスポンシブUIとアクセシビリティ

- スマートフォン幅でもフォームが横にはみ出さないこと。
- ラベル、見出し、フォームの対応関係を明確にすること。
- 関連する入力群には `fieldset` と `legend` を使用すること。
- 位置情報の状態メッセージは `role="status"` などで読み上げ可能にすること。
- ボタン、リンク、入力欄のフォーカス状態を視認できるようにすること。
- 色だけでエラーや状態を表現しないこと。
- 既存デザインがある場合は、検索画面と結果画面だけが浮かないように統一すること。

## 実装手順

1. リポジトリの構成、既存ルート、テンプレート、スタイル、データ形式を調査する。
2. 既存の検索・避難所機能があれば重複実装せず、必要な部分を拡張する。
3. バックエンドに検索関数と検索結果ルートを実装する。
4. 検索画面テンプレートを実装する。
5. 結果画面テンプレートを実装する。
6. 共通ナビゲーションと必要なCSSを更新する。
7. 既存機能に影響がないことを確認する。
8. テストまたは手動確認を実行し、確認結果を最後に報告する。

## 受け入れ条件

以下をすべて満たした状態を完成とします。

- `/shelter_search` が表示できる。
- 簡易検索ボタンから現在地を取得し、緯度経度付きで検索できる。
- 位置情報未対応・拒否時に画面内へ日本語メッセージが表示される。
- 現在地検索の結果が距離順になる。
- 避難所名の部分一致検索ができる。
- 配慮事項を1つ選んで絞り込める。
- 配慮事項を複数選んだ場合にAND条件になる。
- 出発地点の3つの選択肢が表示される。
- 結果件数、避難所名、住所が表示される。
- 住所などの欠損データで表示が壊れない。
- 0件でもエラー画面にならず、専用メッセージが表示される。
- 全避難所一覧を表示できる。
- 検索結果から検索画面とトップページへ戻れる。
- スマートフォン幅でレイアウトが崩れない。
- 既存の無関係な機能が動作する。

## 最後の報告

実装後は、変更したファイル、追加したルートや検索条件、実行したテスト・確認内容、未実装またはデータ不足の点を簡潔に報告してください。実装を完了せずに設計案だけを返さないでください。
